import React from "react";

/**
 * This module contains logics about react context for car warranty.
 */

/**
 * the React context, so that information can be conssumed by every components
 * over the car warranty booking.
 */
const WarrantyContext = React.createContext();

export default WarrantyContext;
